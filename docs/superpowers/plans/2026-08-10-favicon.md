# Minimal M Favicon Implementation Plan

> **For agentic workers:** Implement inline in the current task. Do not commit or push; the repository rules reserve those actions for the user.

**Goal:** Replace the illustrated browser favicon with a legible adaptive `M` lettermark.

**Architecture:** Add one dependency-free SVG asset and point the existing Jekyll favicon configuration at it. Reuse the theme's current head include instead of adding new link-tag logic.

**Tech Stack:** SVG, Jekyll YAML configuration, Sharp for raster inspection, existing Jekyll build tooling.

## Global Constraints

- The letter is uppercase `M`; do not introduce `SM`, `S.M.`, or `MIN`.
- Use `#00ab37` only for the upper-right terminal.
- Preserve all page layout, navigation, content, and wordmark behavior.
- Do not commit or push.

---

### Task 1: Create and connect the favicon

**Files:**
- Create: `assets/img/favicon.svg`
- Modify: `_config.yml`

**Interfaces:**
- Consumes: the existing `site.icon` branch in `_includes/head.liquid`
- Produces: `/assets/img/favicon.svg` as the browser favicon URL

- [x] **Step 1: Add the SVG lettermark**

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <style>
    .mark { fill: #111111; }
    @media (prefers-color-scheme: dark) { .mark { fill: #f4f4f4; } }
  </style>
  <path class="mark" d="M8 52V12h10l14 20 14-20h10v40H46V29L32 47 18 29v23z"/>
  <path fill="#00ab37" d="M46 12h10v10H46z"/>
</svg>
```

- [x] **Step 2: Point the existing config at the SVG**

Change `_config.yml` to:

```yaml
icon: favicon.svg # the emoji used as the favicon (alternatively, provide image name in /assets/img/)
```

- [x] **Step 3: Inspect small-size renders**

Render the SVG at 16 px and 32 px on light and dark backgrounds with Sharp. Confirm that the `M` remains open through the center and the green terminal does not become the dominant shape.

- [ ] **Step 4: Build and inspect the generated favicon link**

Run the repository's existing Jekyll build command. Inspect generated HTML and confirm its shortcut-icon URL contains `/assets/img/favicon.svg`.

Environment note: Ruby and Bundler are not installed on this host, and the local Docker daemon is not running. The existing Liquid branch and expected asset URL were checked statically; a full Jekyll build remains unverified.

- [x] **Step 5: Review the diff**

Confirm the functional diff is limited to `assets/img/favicon.svg` and `_config.yml`; documentation files only record the approved design and plan.
